export const API_URL = 'http://localhost:5000'

export const API_PATH = {
  fit: {
    home: `${API_URL}/`
  }
}

export const INITIAL_STATE = {

  logged_user: {},

  layout: {
    loading: false
  }
}